# CheburMail

**Мессенджер со сквозным шифрованием для Android, работающий поверх обычной электронной почты.**

Никаких своих серверов. Не нужен номер телефона. Сообщения идут через стандартный IMAP/SMTP (Яндекс Почта, Mail.ru), но почтовый провайдер видит только метаданные — никогда содержимое.

[English README](README.md)

## Почему CheburMail, а не Signal / Telegram / WhatsApp?

| | CheburMail | Signal | Telegram | WhatsApp |
|---|---|---|---|---|
| **Работает в РФ без VPN** | Да — Яндекс/Mail.ru в белых списках | Заблокирован с 2024, нужен VPN | Частично заблокирован, нестабилен | Работает, но серверы Meta могут заблокировать |
| **Переживёт «чебурнет»** | Да — почта это внутренняя инфраструктура | Нет — зависит от зарубежных серверов | Нет — серверы за рубежом | Нет — серверы за рубежом |
| **Нужен номер телефона** | Нет — только почтовый аккаунт | Да | Да | Да |
| **Свои серверы** | Нет — работает поверх почты | Серверы Signal Foundation | Серверы Telegram | Серверы Meta |
| **Сквозное шифрование** | Да (X25519 + XSalsa20-Poly1305) | Да (Signal Protocol) | Только «секретные чаты» | Да (Signal Protocol) |
| **Открытый код** | Да | Да | Только клиент | Нет |
| **Можно заблокировать одним доменом** | Нет — работает через любого российского почтовика | Да | Да | Да |

**Суть**: Яндекс Почта и Mail.ru — часть утверждённой коммуникационной инфраструктуры страны. Их заблокируют последними — если вообще когда-нибудь. CheburMail использует их как транспорт, шифруя всё на стороне клиента. Провайдер видит кто с кем переписывается, но не содержимое.

**«Чебурнет»** — разговорное название суверенного интернета, при котором зарубежные сервисы отключены. В этом сценарии Signal мёртв, Telegram нестабилен, а **почта работает**. CheburMail создан для этого сценария.

## Как это работает

CheburMail превращает вашу почту в защищённый канал связи:

1. **Входите** под своей Яндекс или Mail.ru почтой (пароль приложения)
2. **Обмениваетесь ключами** — сканируете QR-код при личной встрече
3. **Сообщения шифруются** на устройстве перед отправкой — провайдер видит только набор символов
4. **Доставка через IMAP/SMTP** — без промежуточных серверов, без регистрации

```
Вы (шифрование) → SMTP → Почтовый провайдер → IMAP → Получатель (расшифровка)
                          ↑ видит только метаданные
```

## Безопасность

| Уровень | Технология |
|---------|-----------|
| Обмен ключами | X25519 (Curve25519 Diffie-Hellman) |
| Шифрование | XSalsa20-Poly1305 (аутентифицированное) |
| Хранение ключей | Android Keystore + Tink AEAD |
| Локальная база | SQLCipher (AES-256), парольная фраза в зашифрованном DataStore |
| Реализация | libsodium через lazysodium-android |
| Медиа | Отдельный ключ на каждый файл |

- **Zero-knowledge**: почтовый провайдер не может прочитать сообщения
- **Без своих серверов**: нечего взломать, заблокировать или запросить по решению суда
- **Шифрование at rest**: контакты, чаты, сообщения, отпечатки — всё лежит в SQLCipher-базе; с украденного или рутованного устройства её не прочитать без привязанной к приложению парольной фразы
- **Уникальный nonce** на каждое сообщение
- **Верификация**: сравнение отпечатков ключей через QR-код или вручную по 60-значному коду

## Возможности

### Работает
- **Личные чаты** со сквозным шифрованием
- **Групповые чаты** со сквозным шифрованием (fan-out: каждому участнику отдельный шифртекст, общего ключа группы нет)
- **Добавление в группу с подтверждением админа** — если не-админ хочет добавить участника, админ получает запрос с отпечатком ключа новичка и подтверждает вручную, только после этого рассылается инвайт
- **Медиа-сообщения**: фото, файлы, голосовые — всё зашифровано
- **Обмен ключами через QR-код** — сканируете при встрече, без серверов
- **Обмен ключами вручную без QR** — ввод короткого кода, когда сканировать нечем
- **Верификация отпечатков ключей** — 60-значный код безопасности (QR или вручную)
- **Автоматический re-keyex после переустановки** — приложение понимает, что у контакта пропали ключи, и переобменивается с ним заново, защищая от спама ограничителем частоты (`ReactiveKeyexGate`)
- **Ответ, цитирование, удаление** сообщений (удаляет и из IMAP)
- **Переименование чатов**
- **Фоновая синхронизация** через WorkManager + IMAP IDLE
- **Защита от скриншотов** на экранах чатов
- **Зашифрованная локальная БД** (SQLCipher) с автоматической миграцией старых plaintext-установок
- **Без трекинга, аналитики и рекламы**

- **PIN / биометрия** — защита входа PIN-кодом или отпечатком пальца
- **Автоочистка IMAP** — автоматическое удаление обработанных писем старше 7 дней
- **Проверка обновлений** — уведомление о выходе новой версии (отдельные каналы debug / release)

### В разработке
- **Мульти-аккаунт** — переключение между почтовыми ящиками
- **UI для повторного приглашения** участника группы после переустановки приложения
- **Tombstone для удалённых групп** — чтобы архивные IMAP-письма не воскрешали удалённый чат

### Сознательно не делаем
- **Статусы доставки / прочтения** и **исчезающие сообщения** — и то и другое требует отправки дополнительных служебных писем по IMAP/SMTP на каждое сообщение, что прямо противоречит идее CheburMail (минимум трафика и метаданных на стороне провайдера). Не реализуем.

## Поддерживаемые провайдеры

| Провайдер | IMAP | SMTP |
|-----------|------|------|
| Яндекс Почта | `imap.yandex.ru:993` | `smtp.yandex.ru:465` |
| Mail.ru | `imap.mail.ru:993` | `smtp.mail.ru:465` |

> Нужно создать **пароль приложения** в настройках безопасности почты. CheburMail никогда не видит ваш основной пароль.

## Сборка из исходников

**Требования**: JDK 17, Android SDK (API 35)

```bash
export ANDROID_HOME=/path/to/android-sdk
export JAVA_HOME=/path/to/jdk-17

git clone https://github.com/shizzka/cheburmail.git
cd cheburmail
./gradlew assembleDebug --no-daemon
# APK → app/build/outputs/apk/debug/app-debug.apk
```

## Стек

- **Язык**: Kotlin 2.3
- **UI**: Jetpack Compose + Material 3
- **Крипто**: lazysodium-android (libsodium) + Google Tink
- **Транспорт**: JavaMail (IMAP/SMTP)
- **Хранение**: Room поверх SQLCipher + Encrypted DataStore
- **Синхронизация**: WorkManager + IMAP IDLE foreground service
- **QR**: ZXing (генерация) + Google Code Scanner (сканирование)
- **Min SDK**: 26 (Android 8.0)

## Архитектура

```
┌─────────────────────────────────────────────┐
│                 UI-слой                      │
│        Jetpack Compose + ViewModels          │
├──────────┬──────────┬───────────┬────────────┤
│  crypto/ │transport/│  storage/ │   sync/    │
│          │          │           │            │
│ X25519   │ SMTP     │ Room+     │ WorkManager│
│ XSalsa20 │ IMAP     │ SQLCipher │ IMAP IDLE  │
│ libsodium│ JavaMail │ Tink AEAD │            │
└──────────┴──────────┴───────────┴────────────┘
         ↕                    ↕
  Почтовый провайдер    Локальное хранилище
  (Яндекс/Mail.ru)    (зашифрованные ключи)
```

## Формат сообщений

Сообщения используют свой формат, невидимый обычным почтовым клиентам:

- **Subject**: `CM/1/<chatId>/<messageUuid>`
- **Body**: `Base64(nonce || ciphertext)`
- **Content-Type**: `application/x-cheburmail`

## FAQ

**Почему email?**
Email — это федеративная, проверенная временем инфраструктура, которая уже есть везде. Не нужно поднимать серверы, нет затрат на инфраструктуру, нет единой точки отказа.

**Может ли почтовый провайдер прочитать мои сообщения?**
Нет. Сообщения шифруются на устройстве до того, как попадут на почтовый сервер. Провайдер видит отправителя, получателя и время — но содержимое неотличимо от случайных данных.

**Что будет, если я потеряю устройство?**
Потеряете сообщения и ключи. Это сделано намеренно — бэкапа нет, серверного восстановления нет, извлечь ключи невозможно. Если кто-то украдёт телефон, контактам достаточно заново обменяться ключами с вашим новым устройством; локальная база зашифрована SQLCipher, так что вор не прочитает переписку даже с root-доступом.

**А если кого-то добавят в групповой чат без моего согласия?**
Не смогут. Добавлять новых участников может только админ группы. Если не-админ пытается добавить — админу приходит запрос с отпечатком ключа новичка, и без его явного подтверждения инвайт в группу не уходит.

**Можно ли использовать Gmail / Outlook?**
Пока нет. Сейчас поддерживаются Яндекс Почта и Mail.ru. Добавить новых провайдеров несложно — PR приветствуются.

## Скачать

- **Telegram-бот**: [@my_fabrica_bot](https://t.me/my_fabrica_bot) — нажмите «Скачать APK»
- **GitHub Releases**: [последний релиз](https://github.com/shizzka/cheburmail/releases/latest)

## Лицензия

MIT
